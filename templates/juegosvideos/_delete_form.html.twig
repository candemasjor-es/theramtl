<form method="post" action="{{ path('app_juegosvideos_delete', {'id': juegosvideo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ juegosvideo.id) }}">
    <button class="btn">Delete</button>
</form>
